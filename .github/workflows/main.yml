name: main workflow
on: push
jobs:
  Build:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    steps: 
      - name: clone down repository
        uses: actions/checkout@v4
      - run: ci/build-app.sh
